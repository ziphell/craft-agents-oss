{
  "name": "@craft-agent/pi-agent-server",
  "version": "0.13.3",
  "license": "Apache-2.0",
  "description": "Out-of-process Pi agent server communicating via JSONL over stdio",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pi-agent-server": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=bun --format=esm --external koffi",
    "typecheck": "tsc --noEmit -p tsconfig.typecheck.json",
    "validate": "bun run typecheck && bun run build"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-agent-core": "0.85.1",
    "@earendil-works/pi-ai": "0.85.1",
    "duck-duck-scrape": "^2.2.7",
    "node-html-parser": "^6.1.0",
    "pdfjs-dist": "^5.4.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
